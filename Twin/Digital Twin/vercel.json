{
  "buildCommand": "cd mobile_application && npm install && npm run build",
  "outputDirectory": "mobile_application/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
